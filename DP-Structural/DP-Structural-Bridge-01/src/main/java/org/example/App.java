package org.example;

import org.example.normal.database.MYSQLDB;
import org.example.normal.database.OracleDB;
import org.example.normal.interfaces.Database;

/**
 * Hello world!
 *
 */
public class App 
{
    public static void main( String[] args )
    {
        //normal state:::

      /*  Database mySQL = new MYSQLDB();
        mySQL.connect("jdbc:mysql//database//3306",
                "milad",
                "123456");
        mySQL.executeQuery("okay mysql execute query!!!");
        mySQL.disconnect();
        Database oracleDB = new OracleDB();
        oracleDB.connect("oracleDB//database//3306",
                "nader",
                "1234453456");
        oracleDB.executeQuery("okay oracleDB execute query!!!");
        oracleDB.disconnect();*/




    }
}
